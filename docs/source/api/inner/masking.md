# `slsqp_jax.inner.masking`

Shared `make_active_subproblem` helper that masks `(A, b, hvp_fn, g)` down to the active rows / free variables — used identically by every inner solver.

`slsqp_jax.inner.masking`

```{eval-rst}
.. automodule:: slsqp_jax.inner.masking
   :members:
   :undoc-members:
   :show-inheritance:
```
